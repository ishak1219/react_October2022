import React from 'react';
import ReactDOM from 'react-dom';
import RootComponent from './root_component/root';

ReactDOM.render(<RootComponent></RootComponent>, document.getElementById('root'))
